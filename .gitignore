# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Build outputs
node_modules
dist
dist-ssr
*.local

# Lockfiles — intentionally not tracked. Reproducible from config files
# (package.json + Cargo.toml) plus the tooling versions documented in
# CLAUDE.md.
bun.lock
bun.lockb
package-lock.json
yarn.lock
pnpm-lock.yaml
Cargo.lock

# Tauri / Rust
src-tauri/target/
src-tauri/gen/schemas/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?